class Device {
  Device(this.id, this.type, this.title, this.description, this.attached);

  final String id;
  final String type;
  final String title;
  final String description;
  final bool attached;
}
